<h1>Edit Task</h1>

<div class="row">
  <div class="col-md-4">
    <p>Remember you <b>STILL only have 7 days</b> to complete your task!</p>
    <ul>
      <li>Start right now!</li>
      <li>Add a picture to help you remeber.</li>
      <li>Check your email for a daily reminder about your task.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@list, @task] do |f| %>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: "Give your task a name" %>
    </div>
    <div class="form-group">
      <%= f.label :body %>
      <%= f.text_area :body, rows: 2, class: 'form-control', placeholder: "Explain your task" %>
    </div>
    <div class="form-group">
      <%= f.submit "Add to your list", class: 'btn btn-success' %>
    </div>
    <% end %>
  </div>
</div>
